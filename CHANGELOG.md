-   Develop flask web application providing batch maintenance tools for mdwiki.org. [#1](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/1) [#2](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/2) [#3](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/3)

-   Adds a pytest suite covering the jobs runtime, auth surface, every blueprint, and per-service pure logic. [#4](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/4)

-   Added background job system for long-running operations with real-time progress tracking and live updates. [#5](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/5)

-   OAuth-based access control; new API utilities for page edits, moves, and text/category retrieval; Commons client helpers and mwclient wrappers added. [#6](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/6)

-   Launched six new job types: Create Redirects, Duplicate Redirect, Find & Replace, Fix Redirects in All Pages, Normalize References, and Import Page History. [#7](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/7)

-   More robust handling of missing/empty API responses and improved capture/reporting of edit/save outcomes. [#8](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/8)
-   Updated wiki domain configuration to handle missing language and family settings more gracefully, with improved fallback behavior when configuration is incomplete. [#9](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/9)
-   Add job placeholders and reorganize tools UI. [#10](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/10)

-   Full "Add R column" job implemented end-to-end; client-side autocomplete, card tools, and dark-mode support added. [#16](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/16)
-   Improved network request error handling with specific handling for timeouts, HTTP errors, and JSON decode failures. [#17](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/17)
-   More reliable save operations with clearer failure logging and result handling. [#18](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/18)
-   Improved job lifecycle management with standardized worker architecture. [#19](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/19)

-   Simplified cancellation detection and ensured cancellation state is reflected in running jobs. [#20](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/20)
-   Fix job status and cancellation updates in result files. [#22](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/22)
-   Added a dedicated database error page to show a friendly "Database Error!" message when the app cannot initialize the database. [#23](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/23)
-   Added user profile dashboard displaying job statistics and a table of recent jobs with timestamps and quick-view links. [#24](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/24)
-   Login/start button styling standardized to outline variants. CSRF rendering unified across job forms. "Details" links simplified and job listing UI streamlined. [#25](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/25)
-   introduces an admin dashboard and coordinator management functionality. [#26](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/26)
-   Job detail pages now show summary stats as Bootstrap cards and split processed pages into separate cards (changed/fixed vs remaining) with diff links. [#27](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/27)
-   refactors and consolidates the job workers by introducing a shared <code>SharedworkerObject</code> and <code>UpdaterOutcome</code> structure, removing duplicate code across several workers, and updating the corresponding HTML templates. [#30](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/30)
-   introduces defensive exception handling around database operations in several route and worker files. [#32](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/32)
-   introduces a file-based cancellation mechanism for jobs to improve cross-process cancellation detection, alongside general import cleanups and test formatting. [#34](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/34)

-   introduces a periodic database cancellation check for background jobs, refactors job cancellation routes to centralize authorization and lookup logic, and updates templates to use shared macros with customizable columns. [#35](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/35)
-   Improved redirect worker behavior to maintain page content integrity during link updates. [#36](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/36)
-   Better MediaWiki link normalization and improved auth/session handling to reduce failed edits and login errors. [#37](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/37)
-   More reliable login/callback persistence and safer cleanup when users or tokens are removed. Job audit history preserved when user accounts are deleted. [#38](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/38)
-   Improved database initialization error handling with clearer error reporting. [#39](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/39)
-   Refactor internal functions and add explicit module exports. [#40](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/40)
-   Fixed trailing whitespace issues in generated medical content sections. Corrected navigation link routing for the Medical Content Updater tool. [#41](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/41)

*   This pull request refactors user management by extracting user CRUD operations into a new `users_service`, updating the database schema to support auto-incrementing user IDs, and revising the admin routes and templates. Feedback on the changes highlights several critical runtime issues, including references to non-existent service methods and missing model fields in the user management routes, as well as `AttributeError` bugs from calling `list_users` on the wrong service. Additionally, the review points out template bugs such as duplicate HTML IDs and missing table tags in `users.html`, potential `ValueError` exceptions and inefficient string manipulation in the `newupdater` route, and opportunities to improve status reporting in the redirect creator worker. [#42](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/42)
*   This pull request introduces user permissions (can_run_jobs and can_run_bg_jobs) to control synchronous and background job execution, along with admin routes to toggle these settings. It also lays the groundwork for a refactoring plan by adding an is_active_admin field to CurrentUser. The code reviewer provided several actionable suggestions, including ensuring type consistency by using booleans instead of integers for permission updates, simplifying ternary expressions, and using safe dictionary access to prevent potential KeyErrors in the redirect worker. Additionally, the reviewer noted that the newly added is_active_admin field is not yet populated or utilized to optimize admin checks as described in the refactoring plan. [#43](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/43)
*   This pull request removes the unused `active_coordinators` function and its references, replacing them with `is_active_coordinator` across the codebase, documentation, and tests. It also refactors route logic, such as simplifying trailing slash removal in `newupdater/route.py` and renaming variables in the coordinators dashboard. Feedback was provided regarding a bug in `fixred.py` where `request.form` is incorrectly used to retrieve the `save` parameter during a GET request in the `index` function, which should be corrected to use `request.args`. [#44](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/44)
*   This pull request refactors the user token service by renaming `upsert_user_token` to `update_user_token` and removing the insertion logic, making it an update-only operation. It also improves error handling in `save_and_get_user` by breaking it down into granular try-except blocks, reorganizes settings configuration, and updates corresponding tests. No review comments were provided, so there is no additional feedback. [#45](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/45)
*   This pull request introduces a mechanism to prevent starting multiple background jobs of the same type concurrently by checking for existing 'pending' or 'running' jobs, raising a new `JobAlreadyRunningError` exception, and displaying a warning flash message to the user. While the implementation includes comprehensive unit and integration tests, the review feedback highlights a critical check-then-act race condition in `start_job` that could allow concurrent requests to bypass the check. It is highly recommended to enforce this constraint at the database level using a partial unique index to guarantee mutual exclusion. [#47](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/47)
*   This pull request implements duplicate job prevention by introducing a partial unique index on the jobs table for active (pending or running) jobs, along with an application-level check (`has_active_job`) to ensure compatibility across database backends like MySQL. It also defines a custom `DuplicateJobError` exception that propagates from the service layer through the worker to the route handler, where a warning is flashed to the user. Comprehensive unit and integration tests have been added to verify this behavior. The review feedback provides valuable improvements, including using SQLAlchemy's `@compiles` extension to prevent the partial index from being created as a plain unique index on MySQL, correcting the `sqlite_where` definition to use `text()`, adding a composite index to optimize active job queries, and strengthening the integration test assertions to verify the flashed warning message content. [#48](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/48)
*   This pull request introduces a mechanism to prevent duplicate concurrent jobs of the same type by adding an `is_running` column and a unique constraint on `(job_type, is_running)` in the `JobRecord` model, along with a new `DuplicateJobError` exception and corresponding test coverage. Feedback on these changes highlights a potential `IntegrityError` if `create_job` is called with a `None` username when the column is non-nullable, suggests adding a `"danger"` category to a flash message, and recommends updating the docstring of `has_active_job` to clarify that the database constraint is the primary enforcement mechanism.
*   This pull request introduces several improvements and fixes, including a short-circuit check in the user session loading logic when a user ID cannot be resolved, corrected exception logging in coordinator routes, and improved SQLite foreign key enablement error handling. Additionally, it includes minor docstring formatting, sorted export lists, and reordered field updates during job cancellation. Feedback on these changes suggests catching the base `sqlite3.Error` instead of `sqlite3.DatabaseError` to ensure all SQLite-specific exceptions are properly caught and wrapped.. [#50](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/50)
*   This pull request refactors the sidebar generation logic to pass structured items, introduces a new `priority_per_item` configuration option for jobs, and updates several job templates to inherit from public base templates. The review feedback suggests improving type safety by updating the signature of `_env_int` to accept `int | None` and adding type annotations to `generate_list_item`. Additionally, it is recommended to use an explicit `is not None` check for `priority_per_item` to avoid ignoring a valid priority value of `0`.. [#51](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/51)
*   This pull request introduces several updates, including exposing new API services, adding a `short_url` Jinja filter, applying minor CSS styling adjustments, and updating flash message layouts and URL encoding in templates. The review identified three key issues: removing the Flask-to-Bootstrap category mapping in `_macros.html` will break styling for default flash categories like `message` or `error`; the `short_url` filter can fail if query parameters contain slashes, so query strings should be stripped first; and setting a hardcoded `16px` font size on the `html` element overrides user browser preferences, which should be changed to `100%` for accessibility.. [#52](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/52)

*   This pull request refactors job management routes and background worker logic, including replacing Flask's global `g` with a `load_user` utility, improving error handling for job deletion, and adding a route to view expanded job details. Feedback on the changes points out a duplicate route path conflict that makes the new endpoint unreachable, as well as an incorrect docstring in the background runner function.. [#53](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/53)
*   This pull request introduces a new Flask CLI skill documentation file, refactors page editing APIs to include field validation, updates job detail expansion routes, and standardizes the worker result attribute name from `result_object` to `result` across workers and tests. Additionally, UI enhancements were made to Jinja macros and job lists. The review feedback highlights a mismatch in the `render_step` macro where `step.msg` is used instead of `step.message`, and a type annotation mismatch in the `_start` method of the `AddRColumnWorker` which is annotated as returning `None` but returns booleans.. [#54](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/54)
*   This pull request refactors the background job system by renaming the `new_jobs` module to `public_jobs`, restructuring the workers under `jobs_workers`, and introducing CLI job execution capabilities. It also updates several templates, macros, and third-party library versions (Bootstrap and DataTables). The review feedback identifies critical issues that need to be addressed: a potential Path Traversal vulnerability in the job result file reading route, a missing User-Agent header in the cronjob site client initialization (violating Wikimedia policy), lost traceback context in exception handling within the CLI job runner, misleading log messages, and a consistent typo in the newly introduced form macros.. [#55](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/55)
*   This pull request introduces a collapsible table feature for jobs with more than 100 entries by adding a new `_skipped_table.html` template and updating various job macros and detail templates to support expansion. The review feedback highlights several improvement opportunities: several detail templates should be updated to use the new `pages_skipped_table` macro instead of `template_pages_errors` to prevent empty columns; default values of `False` should be added to the `expand_all` parameters for consistency; invalid HTML nesting of an anchor tag inside a button should be resolved; and the wiki link in the skipped table should fall back to `page.template_title` to prevent broken URLs.. [#56](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/56)
*   This pull request refactors the `MwClientPage` wrapper by splitting it into separate module files, unifying its retry logic, and updating page creation/editing methods to use boolean flags instead of integers. Feedback highlights two key issues: first, the `stop_nets` fixture in `tests/conftest.py` needs to explicitly call `enable_socket()` for network-marked tests to prevent persistent socket disabling; second, the `get_redirect_target` method in `mwclient_wraper.py` contains a type mismatch (returning a `Page` object instead of a string), uses exceptions for normal control flow, and lacks exception handling for the `page.exists` check.. [#57](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/57)
*   This pull request refactors the API services by removing the pages_api.py module and migrating its procedural functions (such as is_page_exists, get_page_text, and edit_page) to object-oriented methods on the MwClientPage class. Consequently, multiple job workers and shared services have been updated to use MwClientPage. Feedback on these changes includes: ensuring get_category_members_api defaults cmlimit to 'max' or 500 when limit is None to avoid inefficient API paging; logging page existence check exceptions as warnings rather than debug; removing a redundant exists() check in get_text(); simplifying logger.exception by removing the redundant exc_info parameter; and checking if text is None instead of if not text in edit() to allow valid page blanking operations.. [#58](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/58)
*   This pull request introduces UI enhancements, including dynamic title blocks, new job action macros, a popup window helper, and a diff link for completed steps, alongside significantly expanded unit test coverage for various job workers. Review feedback highlights opportunities to improve macro reusability by avoiding hardcoded domains, add safety checks to the popup window function, and remove a redundant title block override. Additionally, the reviewer notes visual regressions in the job action buttons and suggests parameterizing button classes in the macros, while also recommending test improvements to assert thread execution and prevent global state leakage.. [#59](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/59)
*   This pull request introduces a new background worker `add_rtt_template` to automatically add the `{{RTT}}` template to pages in `Category:RTT`. It also refactors the job listing system by splitting jobs into `jobs_data_for_all_pages` and `jobs_data_one_page`, updating the main index page to render these dynamically. Feedback on the changes highlights several critical issues in the new worker, including a case mismatch when checking for existing templates, potential bugs in the template insertion logic (such as case-sensitivity and global replacement risks), and a type mismatch when filtering pages to add. Additionally, rendering `base.html` directly in the error handlers is flagged as a bad practice, with a recommendation to use a dedicated error template instead.. [#60](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/60)
*   This pull request restructures the application by moving the main application code to the `src/` directory, removing the `ALLOWLIST_USERS` configuration, and significantly expanding unit test coverage across several modules. It also updates `tests/conftest.py` to automatically initialize an in-memory SQLite database and introduces new test fixtures while removing the unused `stub_service` fixture. Feedback on these changes suggests correcting a pluralization typo in `CLAUDE.md` regarding the `csrf_token` fixture, removing the redundant `client` fixture from `conftest.py`, and removing `autouse=True` from the `setup_db` fixture to prevent unnecessary database setup overhead during pure unit tests.. [#61](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/61)
*   This pull request introduces the `db_guard` decorator to the `delete_user` function and adds race condition handling to `create_user` by querying for an existing user if a database commit fails. The review feedback recommends catching `IntegrityError` specifically from SQLAlchemy instead of a broad `Exception` to handle the concurrent user creation safely and avoid secondary errors during database failures.. [#62](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/62)

*   This pull request primarily adds explicit `__all__` exports to numerous Python modules to clearly define their public APIs. It also updates development documentation in `README.md`, formats `pyproject.toml`, and modifies `_works_files/add_all.py` to skip files with a main entry point. A review comment suggests improving the main entry point check in `_works_files/add_all.py` to support both single and double quotes.. [#63](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/63)
*   This pull request introduces Pyright configuration to the project, enhances logging formats to include file names and line numbers, and modernizes the SQLAlchemy models to use the SQLAlchemy 2.0 declarative style with `Mapped` and `mapped_column`. Feedback on these changes includes: enabling general type issues in Pyright to ensure effective static analysis, correcting a typo in the log format specifiers (`%(levelname)-s` should be `%(levelname)-8s` to avoid misalignment), and removing redundant `onupdate` parameters in the database models where `server_onupdate` is already configured.. [#64](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/64)
*   This pull request introduces various type annotations, safety checks, and code cleanups across multiple modules, including logging configuration, text processing, and database/user services. The feedback suggests several optimization opportunities, such as eliminating redundant double lookups (using has_arg followed by get_arg), simplifying an unnecessary enumerate loop, and removing a redundant str() cast.. [#65](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/65)
*   This pull request adds a Pyright configuration file and type stub files (.pyi) for the mwclient library to support static type checking. The review feedback suggests enabling core type-checking in Pyright by setting reportGeneralTypeIssues to true, and recommends using relative imports instead of absolute imports across the stub files to ensure consistent and robust type resolution.. [#66](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/66)
*   This pull request introduces several refactorings and improvements, including renaming the `_can_manage_job` helper to `can_manage_job`, updating the `/read-job-result-file` route to `/job-file`, and adding a new `is_job_running` Jinja filter. It also adds type annotations to Python and JavaScript files, and updates the signature of `get_template_pages`. However, in `base_details_public.html`, the `extra_js` block was accidentally removed, which breaks the template structure and prevents child templates from extending it. This block should be restored to wrap the conditional script inclusion.. [#67](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/67)
*   This pull request introduces type annotations across multiple files to support stricter type checking, updates pyproject.toml to elevate several type-checking diagnostics to warnings, and replaces a custom caching implementation in make_title with @functools.lru_cache. The review feedback recommends using more specific type annotations (such as str or bool instead of Any) to resolve type mismatches and improve type safety. It also points out PEP 8 style violations regarding spaces around default argument values with annotations, and highlights a potential test pollution issue in unit tests due to the global lru_cache on make_title, suggesting that the cache be cleared between tests.. [#68](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/68)
*   This pull request introduces various type annotations, safety checks, and refactoring across several modules, including making the blueprint name dynamic in JobsPublicRoutes and cleaning up job worker initializations. Feedback on these changes highlights a critical runtime issue where self.result assertions in before_run() will always fail due to being initialized to None. Additionally, hardcoded blueprint names in redirects will cause failures when dynamic names are used, and there are several minor type annotation and code style issues, such as incorrect \*\*kwargs typing in BaseModel, unidiomatic type unions in add_rtt.py, and type confusion in load_job_result.. [#69](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/69)
*   This pull request introduces a new background worker, `NewUpdaterAllWorker`, designed to run the Medical content updater on all pages within `Category:RTT`. It registers the new job in the public workers list, adds a corresponding HTML template for the UI, and includes basic unit tests. Feedback on the changes suggests two key improvements: first, checking database cancellation periodically (e.g., every 100 pages) even when pages are skipped to ensure the job remains stoppable; second, expanding the unit tests to cover the actual processing logic, text modifications, and page outcomes using mocked dependencies.. [#70](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/70)
*   This pull request refactors `base_worker_object.py` to initialize `self.result` directly in `__init__`, enabling the removal of multiple redundant assertions. It also updates exclusion paths in `pyproject.toml` and introduces various linting suppressions in the test suite. Feedback suggests updating `self.result.started_at` during `before_run()` to ensure accurate execution timestamps, asserting specific exceptions in tests rather than using generic ones, and removing unused imports instead of globally suppressing the `F401` linting rule.. [#71](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/71)
*   This pull request centralizes deletion logic by introducing a new `delete_service.py` and refactoring existing database services and tests to use it. However, several issues were identified in the review: the removal of `setup_logging` in `logger_config.py` completely disables application logging; using `db.session.get()` followed by `db.session.delete()` in the generic deletion helper introduces a performance regression compared to query-level deletes; and `delete_user_token` may incorrectly attempt to delete by a surrogate primary key instead of `user_id`.. [#73](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/73)

*   This pull request refactors job status updates by consolidating status handling, adding case-insensitive status checks, and implementing a retry mechanism for database connection failures (such as MySQL server going away). It also removes `csrf_token` from job arguments before starting background threads. The review feedback highlights several critical improvements: normalizing the status to lowercase to prevent issues with case-sensitive checks elsewhere, removing the `@db_guard_rollback` decorator from `_update_status` to ensure exceptions propagate to the retry handler, using `getattr` to safely check for `connection_invalidated` on `OperationalError`, and avoiding in-place mutation of the `args` dictionary to prevent side effects in calling code.. [#74](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/74)
*   This pull request refactors database retry logic by introducing a @retry_on_db_disconnect decorator, renames the 'scanned' metric to 'processed' across multiple job workers and templates, and reorganizes the user service module with expanded unit tests. The review feedback identifies a bug in the Jinja templates when 'scanned' is 0, warns that other un-updated templates still reference 'scanned', suggests adding type annotations to the new decorator, and notes that calling db.session.remove() in the retry loop could detach SQLAlchemy objects and cause errors.. [#75](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/75)
*   This pull request renames the "app" fixture to "mock_app" across the test suite and introduces comprehensive unit tests for several modules, including category helpers, cookie clients, user token services, and database extensions. The review feedback is highly constructive: it identifies a logical flaw in the new cookie client tests where a 404 status code is falsely used to validate header stripping, points out a redundant subclass assertion, and recommends refactoring repeated @patch decorators into a reusable pytest fixture to align with repository guidelines.. [#76](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/76)
*   This pull request introduces an admin-only application settings management system, enhances the admin dashboard to display recent jobs, adds a rate-limited file download utility, and restructures database decorators. The review feedback highlights several critical issues: `delete_setting` incorrectly passes a string key to a primary-key-based deletion function; a call to `cancel_job_worker` is missing a required argument; string settings with `None` values are incorrectly serialized to the literal string `'None'`; parsing the `Retry-After` header directly as an integer can crash the download process if an HTTP-date is returned; and calling `db.session.remove()` in the database retry loop causes `DetachedInstanceError` by detaching active ORM objects.. [#78](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/78)
*   This pull request refactors the background job route handlers by extracting duplicate logic from the admin and public job route modules into a shared utility module (`jobs_routes_utils.py`). It also updates the corresponding integration and unit tests to reflect these changes. The feedback highlights an inconsistency in `delete_job_handler` where the retrieved `job` object is not passed to `cancel_job_worker`, unlike in `cancel_job_handler`, which could lead to incomplete cancellation logic.. [#79](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/79)
*   This pull request refactors the jobs routes by introducing a common base class `JobsBp` to reduce duplication between admin and public job routes, adds a `user_login_required` decorator, and renames user parameters to `auth_payload` in background job functions. The review feedback highlights an Open Redirect vulnerability in the new `user_login_required` decorator when redirecting to `request.referrer` without validation. Additionally, the feedback suggests enhancing the `JobsBp` base class and its subclasses to properly propagate and enforce background job permission checks for public routes.. [#80](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/80)
*   This pull request refactors the OAuth login flow, improves job error handling, adds an optional `--update-all` flag to the CLI job runner, and introduces referrer validation to prevent open redirects. The review feedback highlights opportunities to make the referrer validation more robust against open redirect bypasses, add missing type annotations, include more context in job lookup error logs, and clean up a minor typo in a comment.. [#81](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/81)
*   This pull request updates the test configuration in `tests/conftest.py` to set the `MAIN_DIR` environment variable using the system's temporary directory combined with a hardcoded "test" subdirectory. The reviewer points out that using a hardcoded subdirectory name can cause permission errors or race conditions in shared environments, and suggests using `tempfile.mkdtemp()` to generate a unique directory instead.. [#82](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/82)
*   This pull request updates a Flask skill schema to use Optional[str] and adds comprehensive reference documentation for the mwclient library, covering error handling, image operations, pagination, page operations, and site operations. The review feedback identifies several necessary corrections in the new documentation: correcting the inheritance hierarchy and associated descriptions for the UserBlocked exception, fixing the positional argument order for the email method, resolving invalid Python syntax in redirect resolution examples, and clarifying terminology regarding file-like objects.. [#84](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/84)
*   This pull request reorganizes the codebase by renaming the `app_routes` directory to `public` and consolidating admin routes under `admin/routes`. It also shifts setting-form processing from the database service layer to the admin controller layer, cleans up unused code, and updates corresponding tests and templates. The review feedback points out critical issues in the settings management logic: the return values of `delete_setting_by_key` and `update_setting` are unchecked, which can cause silent failures, and boolean values are serialized as `"True"` instead of `"true"` during setting creation, breaking subsequent parsing.. [#85](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/85)
*   This pull request introduces a detailed Repository Comparison Report (`docs/create.md`) comparing `mdwiki.org_scripts` and `svg_translate_web` to identify consolidation opportunities and a synchronization strategy. The feedback points out a typographical error in the filename `mwclient_wraper.py` which should be corrected to `mwclient_wrapper.py`.. [#86](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/86)
*   This pull request introduces a local development and UI/E2E testing bypass (`UI_TEST_BYPASS_COORDINATOR_CHECK`) for the coordinator check, allowing developers and AI agents to bypass authentication requirements in development environments. It updates configuration settings, environment loaders, and documentation accordingly. The review feedback highlights two key issues: first, `UI_TEST_BYPASS_COORDINATOR_CHECK` is hardcoded to `False` at the class level in `flask_config.py`, which may cause Flask's `from_object` to ignore the environment variable; second, accessing `current_app` in `admin_service.py` without checking `has_app_context()` can lead to a `RuntimeError` when the service is called outside of an active Flask application context.. [#87](https://github.com/Mdwiki-TD/mdwiki.org_scripts/pull/87)
